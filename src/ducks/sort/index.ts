import { default as _sort } from './sort'
import {
  sortResourcesList as _sortResourcesList
} from './sort'

export const sortResourcesList = _sortResourcesList

const sort = _sort
export default sort
