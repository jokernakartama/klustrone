import { default as _resourceDirectory } from './resourceDirectory'
import {
  updateDir as _updateDir,
  getMeta as _getMeta
} from './resourceDirectory'

export const updateDir = _updateDir
export const getMeta = _getMeta

const resourceDirectory = _resourceDirectory
export default resourceDirectory
