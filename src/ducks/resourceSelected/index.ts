import { default as _resourceSelected } from './resourceSelected'
import {
  select as _select,
  deselect as _deselect
} from './resourceSelected'

export const select = _select
export const deselect = _deselect

const resourceSelected = _resourceSelected
export default resourceSelected
