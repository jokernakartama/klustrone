import { default as _keys } from './keys'
import { default as _loc } from './loc'
import { default as _service } from './service'
import { default as _routes } from './routes'

export const keys = _keys
export const loc = _loc
export const service = _service
export const routes = _routes
